
// Array push() Method

function func() {
    let arr = ["GFG", "gfg", "g4g"];
    // Pushing the element into the array
    arr.push("GeeksforGeeks");
    console.log(arr);
}
func();

// Push() adds the numbers to the end of the array.

function funcNum() {
    let arrNum = [34, 234, 567, 4];

    //Pushing the elements 
    console.log(arrNum.push(22, 23, 26));
    console.log(arrNum);
}
funcNum();