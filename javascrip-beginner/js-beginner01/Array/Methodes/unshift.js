// The unshift() method is used to add elements to the front of an Array

// Declaring and initializing arrays

let numArr = [10, 12, 23, 45];
//Adding element at the beginning of an array
numArr.unshift(88, 45);
console.log(numArr);

// Declaring and initializing arrays
let strArr = ["amit", "sumit"];

// Adding element at the beginning of the array
strArr.unshift("sunil", "anil");
console.log(strArr);

// Converting the given array into the strings.
// Original Array
let courses = ["HTML", "CSS", "Javascript", "React"];

// Converting array to string 
let str = courses.toString();
console.log(str)